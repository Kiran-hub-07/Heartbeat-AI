# Heartbeat-AI
# 1. Initialize a clean git directory on your machine
git init

# 2. Track all your local source files (app.py, templates/index.html, etc.)
git add .

# 3. Save your code snapshot locally
git commit -m "Initial commit: Heartbeat AI System complete"

# 4. Rename your main track branch to main
git branch -M main

# 5. Link your local machine folder to your remote GitHub page
# (⚠️ REPLACE the URL below with the exact URL shown on your GitHub page!)
git remote add origin https://github.com/Kiran-hub-07/Heartbeat-AI.git

# 6. Upload your files to GitHub's servers
git push -u origin main
